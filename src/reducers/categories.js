function categories(state = [], action) {
    console.log(state, action);

    switch (action.type) {
        case 'EDIT_CATEGORY':

            break;
        default:

    }

    return state;
}

export default categories;
